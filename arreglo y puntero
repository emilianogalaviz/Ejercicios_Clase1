#include <stdio.h>

int main() {
  int numeros[] = {1, 2, 3, 4, 5};
 int *ptr = numeros; //El puntero apunta al primer elemento del arreglo


  printf( "El valor del primer elemento es: %d\n", *ptr);

  printf( "El valor del segundo elemento es: %d\n", *(ptr + 1));





  return 0;
}
